git init

# Add new files to the staging area
# If there are directories, all items in the directory are added recursively

# Wont work:
git add

# Specify what to add:
git add .

git add *


git status
git commit
git config
git config --list
git config --add email.address "chris.maier@rackspace.com"
git config --add user.name "Chris Maier"
git commit --amend --reset-author
git config --global user.name "Chris Maier"
git config --global email.address "chris.maier@rackspace.com"
git commit --amend --reset-author
git add
git add .
git status
git add .
git status
git config --list
git commit
git status
git status
git add .
git status

# Run git interactively
git add -i



